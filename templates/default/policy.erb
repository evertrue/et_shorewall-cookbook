###############################################################################
#SOURCE   DEST    POLICY    LOG LEVEL LIMIT:BURST
<% node['et_shorewall']['policies'].each do |policy| -%>
<%= policy["source"] %> <%= policy["dest"] %> <%= policy["policy"] %> <%= policy['log_level'] || "" %>
<% end -%>
# THE FOLLOWING POLICY MUST BE LAST
all   all   REJECT    info
