###############################################################################
#INTERFACE    SOURCE    ADDRESS   PROTO PORT(S) IPSEC MARK
<%
  node['et_shorewall']['zone_conf']['zones'].select {|z,z_conf|
    z_conf['masq']
  }.each do |zone,zone_conf|
-%>
<%= zone_conf['interface'] %> <%= zone_conf['masq']['sources'].join(',') %>
<% end -%>
